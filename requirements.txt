# Email delivery + feed/HTTP fetching (always needed)
requests>=2.32.0
feedparser>=6.0.11
python-dateutil>=2.9.0

# Analysis providers — install at least the one matching PROVIDER.
# (Both are light; CI installs both so you can switch via the PROVIDER var.)
google-genai>=1.0.0      # PROVIDER=gemini (default)
anthropic>=0.105.0       # PROVIDER=claude
